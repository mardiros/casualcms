<div class="container is-fluid">
  {% for item in block.items %}
  {% if (loop.index % column_count) == 1 %}
  <div class="columns">
    {% endif %}
    <div class="column">
      {{ include_block(item) }}
    </div>
    {% if (loop.index % column_count) != 0 and loop.last %}
    <div class="column"></div>
    {% endif %}
    {% if (loop.index % column_count) == 0 or loop.last %}
  </div>
  {% endif %}
  {% endfor %}
</div>