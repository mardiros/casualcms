{% extends "layout.jinja2" %}
{% block body %}
  <h1>{{page.hero_title}}</h1>
  {% for p in page.body %}
    <h2>{{p.title}}</h2>
    {{ p.body | safe }}
  {% endfor %}
  {% if related_post_snippet %}
      {{ include_snippet(related_post_snippet) }}
  {% endif %}
{% endblock %}
